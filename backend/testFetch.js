const token = 'ayush_coffee_super_secret_key_2026'; // Not a valid token, we need a real jwt token.
// Actually, I can just use curl with no token if I remove the protect middleware temporarily, but it's easier to view_file server.js to check if the code is actually there.
